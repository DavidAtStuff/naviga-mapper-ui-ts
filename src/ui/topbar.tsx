import React from "react";

export function Topbar(root: any) {
  const element = (
    <div className="topbar">
      <div className="topbar-icon"></div>
      <div className="banner-title">
        <p>Send article to Naviga</p>
      </div>
    </div>
  );
  return element;
}
